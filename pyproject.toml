[project]
name = "matricula-disciplinas"
version = "0.1.0"
description = "Servico de oferta de turmas"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=app --cov-report=term-missing --cov-fail-under=80"
